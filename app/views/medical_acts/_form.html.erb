<%= form_with(model: medical_act) do |form| %>
  <% if medical_act.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(medical_act.errors.count, "error") %> prohibited this medical_act from being saved:</h2>

      <ul>
        <% medical_act.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
